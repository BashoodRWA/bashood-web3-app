import { expect } from "chai";

describe("Scaffold: TestBashoodPresaleFinalMinimal", function () {
  it("placeholder: test file present", async function () {
    expect(true).to.equal(true);
  });
});
