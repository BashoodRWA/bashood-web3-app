const { expect } = require("chai");

describe("BashoodPresaleFinal - payServiceWithBHT (placeholder)", function () {
  it("placeholder test to ensure test runner works", async function () {
    expect(1 + 1).to.equal(2);
  });
});
